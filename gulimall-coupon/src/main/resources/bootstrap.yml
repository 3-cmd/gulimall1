#配置中心的配置文件
spring:
  application:
    name: coupon
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848 #nacos服务地址
      config:
        namespace: 8c5cd069-859c-41db-9590-3b9937d50875
      #配置中心配置要添加的配置服务为什么后缀文件,默认为properties添加如下规则为yaml文件
#      config:
#        file-extension: yaml #文件名后缀